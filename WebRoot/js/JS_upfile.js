 function GetFile(id)
       {
        /*var fileID ="#"+id;
         $(fileID).live('change', function(){
			    var filepath = $(fileID).val();
                var extStart = filepath.lastIndexOf(".");
                var ext = filepath.substring(extStart, filepath.length).toUpperCase();
                if (ext != ".DOC" && ext != ".JPG" && ext != ".GIF" && ext != ".TXT" && ext != ".PDF" && ext != ".RM"&& ext != ".json"&& ext != ".jar")
				{　
				    var fileUpload = document.getElementById(id).value;
				    if (fileUpload != "") 
					{ 
					   var obj = document.getElementById(id) ;
					   obj.outerHTML=obj.outerHTML;
					} 
                    //alert("附件仅限于文件名后缀为.doc、.jpg、.gif、.txt、.pdf、.rm的文件。");
					 
                    return false;
                } 
                var file_size = 0;
                if (!$.browser.msie)
				{
					file_size = this.files[0].size;
                    var size = file_size / 1024;
                    if (size > 2048) 
                    {
						var fileUpload = document.getElementById(id).value;
				        if (fileUpload != "") 
					    { 
					      var obj = document.getElementById(id) ;
					      obj.outerHTML=obj.outerHTML;
					    } 
                        //alert("每个附件仅限2M以内。");
						return false;
                    } 
                }
                return true;
            })*/
       }